# Target database (read-only user strongly recommended)
DB_HOST=postgres_primary
DB_PORT=5432
DB_NAME=tmt
DB_USERNAME=nl2sql_reader
DB_PASSWORD=change_me

# Safety limits
MAX_ROWS=500
STATEMENT_TIMEOUT_MS=15000

# Paths
SCHEMA_FILE=schema/tmt_schema.yml
HISTORY_DB=/data/history.db
INTENT_MODEL_PATH=/data/intent.joblib
